reveal.js v6.0.1 — vendored for offline use.
Files: reveal.js, reveal.css, reset.css (dist/), plugin/notes.js.
Obtained via `npm pack reveal.js`. Theme is custom (see ../index.html), so the
bundled themes/fonts are intentionally not vendored.
