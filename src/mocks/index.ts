/**
 * Throwaway scaffolding that makes the app runnable end-to-end before the real
 * engine exists. Everything here implements a `@domain` port or produces
 * fixture data shaped exactly like the real thing.
 */
export * from './mockEngine';
export * from './fixtures';
