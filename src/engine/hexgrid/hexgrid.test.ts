import { describe, it } from 'vitest';

describe('hexgrid (spec)', () => {
  it.todo('default size over the default extent yields ≈100 cells');
  it.todo('neighbors() are symmetric and number 6 in the interior');
  it.todo('distance() equals hex (cube) distance');
  it.todo('pointToCell() ∘ center is the identity for every cell');
  it.todo('all cell centres fall inside the extent');
});
